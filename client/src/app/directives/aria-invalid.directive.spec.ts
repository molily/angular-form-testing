import { AriaInvalidDirective } from './aria-invalid.directive';

describe('AriaInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new AriaInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
